This project stores data in MySQL (see backend/.env — MYSQL_DATABASE).

The database server holds users, books, chapters, papers, and history.
This folder is only for optional SQL backup files (.sql) if you export them.

It is normal for this folder to be empty when you are not keeping backups.
